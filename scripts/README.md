# scripts/

Developer and operations helper scripts.

| Script | Purpose |
|--------|---------|
| `setup.ps1` / `setup.sh` | Local environment bootstrap (install deps, copy .env) |
| `release.sh` | Version bump + CHANGELOG update + git tag helper |

> Scripts are added as needed. This directory is part of the initial repository
> scaffold.
