{
  "name": "@mrms/realtime",
  "version": "0.1.0",
  "private": true,
  "description": "Typed Socket.IO client + event contracts for MRMS realtime. Sprint 1A ships the typed client factory; UI wiring lands in Sprint 1E.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "lint:fix": "eslint \"src/**/*.ts\" --fix",
    "test": "echo \"@mrms/realtime: client integration tested in Sprint 1E\" && exit 0"
  },
  "dependencies": {
    "@mrms/types": "workspace:*",
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "@mrms/config": "workspace:*",
    "eslint": "^8.57.0",
    "typescript": "^5.5.4"
  }
}
