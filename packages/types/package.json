{
  "name": "@mrms/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared DTO, domain, and enum types for MRMS (single source of truth for cross-app contracts).",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "lint:fix": "eslint \"src/**/*.ts\" --fix",
    "test": "echo \"@mrms/types: no unit tests (pure type declarations)\" && exit 0"
  },
  "devDependencies": {
    "@mrms/config": "workspace:*",
    "eslint": "^8.57.0",
    "typescript": "^5.5.4"
  }
}
