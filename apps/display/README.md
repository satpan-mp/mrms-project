# apps/display

React + TypeScript + Vite kiosk display SPA for MRMS. Renders a room-specific,
fullscreen, TV-optimized display driven by realtime updates (see
`docs/13-UIUX-Wireframe.md`, `docs/14-Component-Hierarchy.md`,
`docs/15-Frontend-Architecture.md`).

> Placeholder. Application code is added in Sprint 5+ (Phase P2).
> No application logic exists yet - repository initialization only.
