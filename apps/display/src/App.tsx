import { RoomDisplayScreen } from './screens/RoomDisplayScreen';

/** Display application root (single kiosk screen in the foundation). */
export function App(): JSX.Element {
  return <RoomDisplayScreen />;
}
